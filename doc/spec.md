# Audio Visual Analyzer 仕様設計書

- Repository: `masa-san-jp/Audio-visual-analyzer-`
- Default branch: `main`
- Document version: `v1.2`
- Date: `2026-04-14`
- Purpose: 開発担当者への引き継ぎ用仕様書

---

## 1. 概要

本アプリケーションは、ブラウザ上で音声ファイルまたは動画ファイルを読み込み、音声解析に応じてリアルタイムにビジュアル表現を行う **オーディオアナライザー / オーディオビジュアライザー** である。

本アプリは以下の2つの利用目的を持つ。

1. **再生モード**
   - 音声や動画を再生しながら、リアルタイムなアナライザー表現を楽しむ
   - 複数スロットを再生キューとして扱う

2. **録画モード**
   - アナライザーの描画を録画し、動画ファイルとして出力する

本アプリは **HTML / CSS / JavaScript を中心に構成し、特別なライブラリのインストールなしで簡単に起動できること** を最重要要件とする。

---

## 2. 開発方針

### 2.1 基本方針
- ブラウザのみで動作する構成を優先する
- 可能な限り **外部ライブラリなし** で実装する
- ユーザーに `npm install` 等を要求しない構成を目指す
- 実装の中核には Web 標準 API を利用する
  - 音声解析: `Web Audio API`
  - 描画: `Canvas 2D API`
  - 録画: `MediaRecorder API`
  - 再生: `HTMLAudioElement` / `HTMLVideoElement`

### 2.2 UI方針
- UI は簡素でよいが、**視認性** と **直感的操作性** を重視する
- ビジュアライザー表示領域を主役とする
- 背景は常に黒固定とする
- コントロール部分は過度な装飾を避け、明快な構成にする

### 2.3 対象環境
- デスクトップブラウザを主対象とする
- 主な対象ブラウザ
  - Google Chrome
  - Microsoft Edge
- 初期段階ではモバイル最適化は優先度を下げる

---

## 3. 想定ユースケース

### 3.1 再生しながら鑑賞する
- ユーザーが音声ファイルまたは動画ファイルを読み込む
- 再生に合わせてビジュアライザーがリアルタイム表示される
- 再生中に色や動き、レイヤー構成を操作できる

### 3.2 複数ファイルを順番に流す
- 3つのファイルスロットにメディアを設定する
- 自動的に順番再生する
- 3番目の再生終了後は1番目に戻る

### 3.3 録画して書き出す
- 単一ファイルでアナライザーを再生する
- ビジュアライザー描画領域を録画する
- 録画結果を動画ファイルとして保存する

---

## 4. 機能一覧

- 音声ファイル入力
- 動画ファイル入力
- リアルタイム音声解析
- 複数種類のアナライザー表示
- アナライザータイプ切替（棒グラフ / 円形放射）
- 表現方法切替（棒 / 波形線 / 点）
- 色・彩度・輝度・変化量のリアルタイム調整
- 音域分割による複数レイヤー表示
- 0dB基準位置の表現切替
- 線の太さ・密度・基準点オフセットの調整
- 色相ランダマイズ機能
- 色相の連続変化モード
- 残像表現
- 3スロット再生キュー
- 再生モード
- 録画モード
- 16:9 / 1:1 表示比率切替

---

## 5. 用語定義

### 5.1 アナライザー
音声信号を解析して可視化する描画表現全般を指す。

### 5.2 レイヤー
音域ごと、または描画設定ごとに分けられた独立表示単位を指す。

### 5.3 0dB地点
可視化上の基準位置・基準辺・中心点を指す。  
厳密な音響計測値ではなく、表示上の「中立位置」の意味で用いる。

### 5.4 スロット
ユーザーがファイルを読み込むための枠。  
再生モードでは3つ存在する。

---

## 6. 画面構成

### 6.1 メイン画面構成
画面は以下の領域で構成する。

1. **ビジュアライザー表示領域**
2. **モード切替・再生制御領域**
3. **ファイルスロット領域**
4. **アナライザー設定領域**（レイヤー設定を含む）

### 6.2 レイアウト方針
- ビジュアライザー表示領域を最も大きく確保する
- 設定パネルは右側または下部に配置する
- 初版はデスクトップ前提の2カラムまたは上下分割構成を推奨する

---

## 7. ファイル入力仕様

### 7.1 入力対象
- 音声ファイル
- 動画ファイル

### 7.2 対応形式
ブラウザが標準的に再生可能な形式を基本対応とする。

#### 音声
- `.mp3`
- `.wav`
- `.ogg`
- `.m4a`
- `.aac`  
※ 一部はブラウザ依存

#### 動画
- `.mp4`
- `.webm`
- `.ogv`
- `.ogg`  
※ `.mov` などはブラウザ依存のため保証対象外

### 7.3 入力方法
- ファイル選択ダイアログ
- 将来的にドラッグ&ドロップを拡張可能とする

### 7.4 バリデーション
- 非対応形式の場合はエラーメッセージを表示する
- 読み込み失敗時は当該スロットにエラー状態を表示する

---

## 8. スロット・再生キュー仕様

### 8.1 スロット数
- 再生モードでは **3スロット**
- 録画モードでは **1スロットのみ有効**

### 8.2 再生順
- スロット1 → スロット2 → スロット3 → スロット1 → ...
- ループ循環を基本動作とする

### 8.3 スロット未設定時の挙動
- 次スロットが未設定なら、その次の設定済みスロットを探す
- すべて未設定なら再生停止する

### 8.4 スロットUI表示項目
各スロットには以下を表示する。
- スロット番号
- ファイル名
- ファイル種別（音声 / 動画）
- 読み込み状態
- 再生対象状態
- 選択ボタン
- 削除ボタン

### 8.5 操作
- 再生
- 一時停止
- 停止
- 次へ
- 前へ
- 対象スロットの手動選択

---

## 9. 音声解析仕様

### 9.1 解析方式
- `Web Audio API` を利用する
- `AudioContext`
- `MediaElementAudioSourceNode`
- `AnalyserNode`

### 9.2 取得データ
- 周波数スペクトラムデータ
- 必要に応じて時系列波形データ

### 9.3 更新方式
- `requestAnimationFrame` を使用して描画同期を行う

### 9.4 基本表現
- 原則として **全音域を1つのアナライザーで表現** する

### 9.6 解析帯域
- アナライザーが対象とする帯域は **50Hz〜15kHz** とする
- 50Hz 未満の超低域ノイズ成分と、15kHz を超える超高域成分は除外する

### 9.5 拡張表現
- 音域を2〜4分割して複数レイヤー表示できる

---

## 10. レイヤー仕様

### 10.1 レイヤー数
選択可能なレイヤー数:
- 1
- 2
- 3
- 4

### 10.2 音域分割
初期実装では以下のような帯域分割を想定する。
- Low
- Low-Mid
- High-Mid
- High

### 10.3 各レイヤーの設定項目
- 表示 ON / OFF
- 対象帯域
- 色相
- 色相変化幅
- 輝度変化幅
- 彩度変化幅
- 感度
- 太さ / サイズ
- 透明度
- 表現タイプ
- レイヤーごとの色相オフセット
- 密度
- 基準点オフセット
- 棒グラフ時のミラー方式
- 残像強度

### 10.4 レイヤー合成
- 同一キャンバス上に重ね描きする
- 描画順はレイヤー番号順を基本とする
- 将来的にブレンドモード追加を可能にする設計とする

---

## 11. アナライザー表示仕様

### 11.1 背景
- 背景色は **常に黒固定**

### 11.2 アスペクト比
切替可能な表示比率:
- `16:9`
- `1:1`

### 11.3 アナライザータイプ
Phase 1・2 では汎用的な表示タイプを扱ってきたが、Phase 3 以降ではアナライザータイプを以下の2種類から明示的に選択可能とする。

- 棒グラフ
- 円形放射

### 11.4 表現方法
各アナライザータイプに対して、以下の表現方法を選択可能とする。

- 棒
- 波形線
- 点

※ アナライザータイプと表現方法を分離して扱える構造を推奨する。  
※ Phase 3 以降では、対応可能な組み合わせを段階的に拡張できる設計とする。

### 11.5 0dB地点表現
0dB地点は、表示上の基準位置としてアナライザータイプごとに以下を採用する。

- 棒グラフ: 底辺基準
- 円形放射: 中心点基準

また、基準点からの表示位置を調整するため、**基準点オフセット** パラメーターを設ける。  
オフセットの最大値は **99** とする。

### 11.6 描画原則
- 再生音声に追従してリアルタイム更新
- 視認性を優先し、背景との十分なコントラストを保つ
- レイヤー数増加時にも破綻しない描画構造とする

### 11.7 棒グラフ時の表示モード（Phase 3 追加仕様）
棒グラフ選択時は、以下の表示モードを選択可能とする。

- 通常
- ミラー（上下対照）
- ミラー（左右対称）

### 11.8 残像表現（Phase 3 追加仕様）
アナライザーに残像表現機能を追加する。

- 最も弱い設定では残像の数は **1つ** で、減衰も早い。
- パラメーターを上げると、残像の数が増え、減衰時間も長くなる。
- 過剰設定では描画が飽和しうるため、以下の方針を採用する。
  - 残像数の上限（最大値）を実装で制御する。
  - 描画飽和が発生しやすい高値域では、ユーザーに注意喚起するか上限でクランプする。
  - 残像の合成は既存描画との輝度加算を基本とし、白飛びが目立つ場合は乗算やアルファ合成も検討する。

---

## 12. インタラクティブ操作仕様

### 12.1 色関連パラメータ
- 色相
- 色相のバリエーション幅
- 輝度の変化幅
- 彩度の変化幅
- 透明度
- 色変化速度
- 色相ランダマイズ（色相 および レイヤーごとの色相オフセットをランダム値に設定）
- アナライザーランダマイズ（タイプ・表現方法・表示モード・レイヤー数・レイヤー色相オフセットをランダム値に設定）
- 形状ランダマイズ（感度・スムージング・線の太さ・密度・基準点オフセット・残像強度をランダム値に設定）
- レイヤーごとの色相オフセット
- 色相連続変化モード（再生中に色相が連続的に変化する）

### 12.2 形状関連パラメータ
- 線の太さ
- 点のサイズ
- 面の広がり
- 高さ / 半径
- 揺らぎ量
- 対称性
- 密度（描画要素数または描画間隔に影響する。最小設定でもアナライザー表示が消失しないことを必須要件とし、実効変化範囲は **100〜30程度** とする）
- 基準点オフセット（最大値 99）
- 残像強度（最小設定では残像1つ・減衰が早い。パラメーターを上げると残像数が増加し減衰時間も長くなる。過剰設定では描画飽和の可能性があるため上限でクランプする）

### 12.3 音反応関連パラメータ
- 全体感度
- レイヤー感度
- スムージング量
- 反応速度
- ピーク保持量

### 12.4 UIコントロール形式
- スライダー
- トグルスイッチ
- セレクトボックス
- 数値入力
- リセットボタン
- ランダマイズボタン（色相 / アナライザー / 形状 の3種類）

### 12.5 操作タイミング
- 再生中もリアルタイム変更可能
- 変更結果は即座に描画へ反映する

---

## 13. 再生モード仕様

### 13.1 概要
- 3スロットキューを利用する通常鑑賞モード

### 13.2 動作
- 選択されたスロットのメディアを再生
- 音声解析を行いビジュアライザーを描画
- 終了時は次スロットへ自動遷移

### 13.3 動画ファイル利用時
- 動画の音声成分を解析対象とする
- 初版では動画映像のメイン表示は不要
- 必要なら小さなプレビュー表示は将来拡張とする

---

## 14. 録画モード仕様

### 14.1 概要
- 単一ファイルを再生しながらビジュアライザー表示を録画する

### 14.2 利用制約
- 利用可能スロットは1つのみ
- キュー再生は無効

### 14.3 録画対象
- ビジュアライザー描画領域のみを録画対象とする
- UIパネルは録画対象外とする

### 14.4 出力形式
- 初版の基本出力は `webm`
- `MediaRecorder` でサポートされる形式を優先する
- `mp4` 出力はブラウザ標準で安定しないため初版対象外とする

### 14.5 操作
- 録画開始
- 録画停止
- 保存
- 再録画

### 14.6 保存ファイル名
- 初期仕様では日時ベースの自動命名でよい
- 将来的に任意名称指定を検討する

---

## 15. UI仕様

### 15.1 必須UI要素
- モード切替
- 再生 / 停止 / 一時停止
- 次へ / 前へ
- スロット選択
- アスペクト比切替
- アナライザータイプ切替（棒グラフ / 円形放射）
- 表現方法切替（棒 / 波形線 / 点）
- 棒グラフ表示モード切替（通常 / ミラー上下 / ミラー左右）
- アナライザーランダマイズボタン
- レイヤー数切替（アナライザーセクション内）
- 色調整UI
- 色相ランダマイズ
- 色相連続変化モード切替
- 感度調整UI
- 線の太さ調整
- 密度調整
- 基準点オフセット調整
- 残像強度調整
- 形状ランダマイズボタン
- 録画開始 / 停止 / 保存

### 15.2 表示ルール
- ビジュアライザー画面は黒背景固定
- ボタン・ラベル・入力欄は暗色背景に対して十分見やすい色を使用する
- コントロール群はカテゴリー別にグループ化する

### 15.3 操作性要件
- 初見でも主要操作が理解できること
- 設定項目は必要最小限から始め、段階的開示できる構成が望ましい
- 重要ボタンの誤操作を減らす配置にする

---

## 16. 非機能要件

### 16.1 起動容易性
- クローン後すぐに利用開始しやすいこと
- できる限りビルド不要構成とする
- ローカルで静的ファイルとして扱える構成を優先する

### 16.2 パフォーマンス
- 一般的なPC環境で滑らかに動作すること
- 1〜4レイヤー構成でも極端に描画が破綻しないこと
- 録画時にも実用的なフレームレートを維持すること

### 16.3 保守性
- 音声処理、描画処理、UI制御、録画処理を分離する
- アナライザータイプ追加が容易な構造とする
- レイヤー機能の拡張がしやすい設計にする

### 16.4 拡張性
将来的に以下の拡張を想定可能な構造にする。
- プリセット保存 / 読込
- ブレンドモード
- 動画プレビュー表示
- 追加アナライザータイプ
- 録画品質設定
- 設定のURL共有やJSON保存

---

## 17. 技術構成案

### 17.1 採用候補
- HTML
- CSS
- JavaScript（Vanilla JS）
- Canvas 2D API
- Web Audio API
- MediaRecorder API

### 17.2 初版で避けるもの
- 重量級フレームワーク
- 必須ビルド工程
- サーバーサイド依存
- 必須の追加ライブラリインストール

### 17.3 将来拡張候補
- WebGL ベース描画
- 軽量UIフレームワーク
- プリセット管理用ストレージ機能

---

## 18. 推奨ディレクトリ構成

```text
/
├─ index.html
├─ style.css
├─ /js
│  ├─ app.js
│  ├─ audio-engine.js
│  ├─ media-manager.js
│  ├─ slot-manager.js
│  ├─ visualizer-core.js
│  ├─ recorder.js
│  ├─ ui-controller.js
│  ├─ settings.js
│  └─ /renderers
│     ├─ dots.js
│     ├─ lines.js
│     ├─ fills.js
│     ├─ bars.js
│     ├─ radial.js
│     └─ mirror.js
└─ /docs
   └─ SPECIFICATION.md
```

---

## 19. モジュール責務

### 19.1 `app.js`
- 全体初期化
- モード管理
- モジュール間連携

### 19.2 `audio-engine.js`
- AudioContext 管理
- AnalyserNode 管理
- FFT データ取得
- レイヤー向け帯域データ分配

### 19.3 `media-manager.js`
- Audio / Video 要素の管理
- ファイル読込
- 再生制御
- 終了イベント処理

### 19.4 `slot-manager.js`
- 3スロット管理
- キュー循環制御
- スロット状態更新

### 19.5 `visualizer-core.js`
- 描画ループ
- キャンバスサイズ管理
- レンダラー切替
- レイヤー合成

### 19.6 `renderers/*`
- 各描画方式の実装
- 共通インターフェースに従う

### 19.7 `recorder.js`
- Canvas キャプチャ
- MediaRecorder 制御
- 保存処理

### 19.8 `ui-controller.js`
- スライダーやボタンイベント管理
- UI と設定値の同期

### 19.9 `settings.js`
- 初期設定定義
- リセット用デフォルト値管理

---

## 20. 開発フェーズ提案

### Phase 1: 最小動作版（完了）
- 単一ファイル読込
- 音声解析
- 基本的な1種類のビジュアライザー
- 再生 / 停止
- 色 / 感度調整
- 16:9 / 1:1 切替

### Phase 2: 表現拡張（完了）
- 複数アナライザータイプ
- 0dB基準位置切替
- 点 / 線 / 面の切替
- レイヤー2〜4対応

### Phase 3: 表現拡張・追加仕様
- アナライザータイプ切替（棒グラフ / 円形放射）
- 表現方法切替（棒 / 波形線 / 点）
- 線の太さ調整
- 密度調整（実効範囲 100〜30、最小でもアナライザーが消えない）
- 0dB基準位置の明確化
  - 棒グラフ: 底辺基準
  - 円形放射: 中心点基準
- 基準点オフセット追加（最大 99）
- 棒グラフの表示モード対応
  - 通常
  - ミラー（上下対照）: 中心線をcanvas垂直中央に固定し上下対称描画
  - ミラー（左右対称）
- 色相ランダマイズ機能（色相およびレイヤーごとの色相オフセットをランダム値に設定）
- アナライザーランダマイズ機能（タイプ・表現方法・表示モード・レイヤー数・レイヤー色相オフセットをランダム値に設定、レイヤー感度は1.0固定）
- 形状ランダマイズ機能（感度・スムージング・線の太さ・密度・基準点オフセット・残像強度をランダム値に設定）
- 解析帯域を 50Hz〜15kHz に限定（超低域・超高域を除外）
- レイヤーセクションをアナライザーセクションに統合
- 色相連続変化モード（再生中に色相が連続的に変化する）
- 残像表現（パラメーターで残像数と減衰時間を制御、過剰設定では上限でクランプ）

### Phase 4: 録画機能（完了）
- 録画モード追加
- webm 保存
- 録画UI整備

### Phase 5: 品質改善
- パフォーマンス改善
- UI整理
- エラーハンドリング強化
- プリセットの検討

---

## 21. 受け入れ条件

以下を満たしたとき、初期仕様を達成したと判断する。

1. 音声ファイルまたは動画ファイルをブラウザで読み込める
2. 再生中にリアルタイムでアナライザーが表示される
3. アナライザータイプを複数から選択できる
4. 再生中に色相・彩度・輝度などを変更できる
5. 1〜4レイヤー構成を切り替えられる
6. 録画モードで単一スロットの録画ができる
7. 録画結果を保存できる
8. 背景が常に黒である
9. 表示比率として 16:9 / 1:1 を選択できる
10. 特別なライブラリ導入なし、または最小限で起動できる

### Phase 3 追加受け入れ条件
11. アナライザータイプとして棒グラフまたは円形放射を選択できる
12. 表現方法として棒 / 波形線 / 点を選択できる
13. 線の太さおよび密度を再生中に変更できる
14. 密度を最小側にしてもアナライザーが消失しない
15. 棒グラフでは通常 / ミラー上下 / ミラー左右を切り替えられる
16. 0dB基準位置に対するオフセットを最大 99 まで調整できる
17. 色相ランダマイズを実行できる
18. 再生中に色相が連続変化するモードを有効化できる
19. 残像表現のパラメーターを調整できる
20. 残像の過剰設定時に描画が飽和しない（上限クランプが機能する）

---

## 22. 制約・注意事項

- 対応メディア形式はブラウザ実装に依存する
- ブラウザの自動再生制限により、初回再生にはユーザー操作が必要
- `MediaRecorder` の出力形式はブラウザ差異がある
- `mp4` の安定出力は標準ブラウザ機能だけでは難しい
- 初版では高機能動画編集ではなく、**アナライザー録画** に目的を限定する

---

## 23. 今後の検討項目

- 描画プリセットの保存
- JSONによる設定入出力
- 動画映像とビジュアライザーの合成表示
- 画質 / フレームレート指定録画
- レイヤーごとのブレンドモード
- 入力デバイス（マイク）対応
- フルスクリーン表示
- キーボードショートカット

---

## 24. 実装優先判断

本プロジェクトでは、以下を優先する。

1. **簡単に起動できること**
2. **リアルタイムで気持ちよく動くこと**
3. **視覚表現を柔軟に変えられること**
4. **録画できること**
5. **UIが直感的であること**

装飾的なUI改善は後回しとし、まずはコア機能の完成を優先する。

---

## 25. 補足

本仕様書は初期設計であり、実装の進行に応じて更新する。  
ただし、以下は固定方針とする。

- 背景は常に黒
- ブラウザ中心実装
- 簡単に起動できることを重視
- 音声 / 動画ファイル入力対応
- 再生モードと録画モードを持つ
- 16:9 / 1:1 対応
